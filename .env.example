# Server-only. Speech-to-text (`/api/transcribe`) and paraphrase (`/api/paraphrase`).
# Never prefix with NEXT_PUBLIC_.
OPENROUTER_API_KEY=

# Optional. Without either key, rewrite uses a local demo transform.
AI_GATEWAY_API_KEY=
OPENAI_API_KEY=
